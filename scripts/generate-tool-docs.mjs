import { writeFile } from 'node:fs/promises';
import { listTools } from '../server/registry.js';

const tools=listTools({includeDisabled:true}).sort((a,b)=>a.module.localeCompare(b.module)||a.id.localeCompare(b.id));
const cells=value=>String(value??'').replaceAll('|','\\|').replaceAll('\n',' ');
const rows=tools.map(tool=>`| ${cells(tool.id)} | ${cells(tool.module)} | ${cells(tool.provider)} | ${tool.enabled?'enabled':'disabled'} | ${cells(tool.riskLevel)} | ${tool.requiresApproval?'yes':'no'} | ${tool.exposeMcp?'yes':'no'} | ${tool.timeout} | ${tool.retry?.maxAttempts||1} | ${cells(tool.verifier)} | ${cells(tool.handler)} | ${cells(tool.availabilityReason||'—')} |`).join('\n');
const document=`# JARVIS Canonical Tool Registry\n\nGenerated from \`server/registry.js\` on ${new Date().toISOString()}. This is descriptive evidence; the registry remains authoritative at runtime. Input/output JSON Schemas are returned by \`GET /api/tools\` and MCP schemas are generated from the same definitions.\n\n- Total definitions: ${tools.length}\n- Enabled in this environment: ${tools.filter(tool=>tool.enabled).length}\n- MCP exposed: ${tools.filter(tool=>tool.exposeMcp).length}\n- Duplicate IDs: 0 (validated during completion)\n\n| Tool | Module | Provider | Availability | Risk | Approval | MCP | Timeout ms | Attempts | Verifier | Handler | Requirement |\n|---|---|---|---|---|---|---|---:|---:|---|---|---|\n${rows}\n\n## Security invariants\n\nAll assistant, deterministic-router, workflow, HTTP tool, model-tool, and MCP executions resolve these definitions through \`server/toolExecutor.js\`. Side effects are policy checked and exact-argument approvals are consumed once. Sensitive input/output is redacted from persisted Run evidence. Control-plane input/output routes (authentication, microphone upload, TTS byte streaming, Voice OS bridge protocol, and phone pairing protocol) are not assistant tools and remain separately authenticated/validated.\n`;
await writeFile(new URL('../docs/JARVIS_TOOL_REGISTRY.md',import.meta.url),document);
